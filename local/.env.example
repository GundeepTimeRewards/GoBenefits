# Copy to .env (or export) for local dev/tests. No AWS required.
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=goben
CONTROL_PLANE_DB=control_plane
AWS_REGION=us-east-1
# DB_SECRET_ARN intentionally unset locally -> creds come from the vars above.
